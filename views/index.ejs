<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <link rel="stylesheet" href="https://bootswatch.com/4/cosmo/bootstrap.min.css">
    <title>Document</title>

</head>
<body>
    <nav class="navbar navbar-expand-lg navbar-dark bg-primary">
  <a class="navbar-brand" href="#">MongoDB</a>
</nav>
<div class="container">
    <div class="row">
        <div class="col-md-5">
            <div class="card">
                <div class="card-body">
                    <form action="/add" method="POST">
                        <div class="form-group">
                            <input type="text" name="title" placeholder="title" class="form-control">
                        </div>
                        <div class="form-group">
                                <textarea  name="description" cols="80" placeholder="title escriba" class="form-control"></textarea>
                            </div>
                        <div class="form-group">
                                <input type="submit" value="up"  class="btn btn-block btn-success">
                         </div>

                    </form>
                </div>
            </div>
        </div>
        <div class="col-md-7">
        <table class="table table-bordered table-hover">
            <thead>
                <tr>
                    <th>num</th>
                    <th>title</th>
                    <th>description</th>
                    <th> action</th>

                </tr>
            </thead>
            <tbody>
                <% for (let i=0; i<task.length; i++){ %>
                    <tr>
                        <td><%= i + 1 %></td>
                        <td><%= task[i].title %></td>
                        <td><%= task[i].description %></td>
                        <td>
                           <a href="/update/<%= task[i]._id %>" 
                        
                            class="btn <%= task[i].status ? 'btn-success' : 'btn-dark' %>">hecha</a>
                            <!-- si es verdadero success sino dark, ventajas de ejs y ecma-->
                            <a href="/editar/<%= task[i]._id %>" class="btn btn-info">modificar</a>
                            <!--Mongo no da id sino _id-->
                            <a href="/delete/<%= task[i]._id %>" class="btn btn-warning">Borrar</a>

                        </td>

                    </tr>

               <% } %>
            </tbody>
        </table>
    </div>
    </div>
</div>
</body>
</html>